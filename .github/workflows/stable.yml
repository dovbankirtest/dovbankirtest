name: Test event
on:
  push:

jobs:
 test_job:
   runs-on: ubuntu-latest
   steps:
      - name: Check out repository code, Clone repo
        uses: actions/checkout@v3

     - name: Run script
       run: scripts/print_something.sh

